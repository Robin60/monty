#include "monty.h"

/**
 * mo_queue - set data format to queue
 * @stack: the double double linked list
 * @line_number: line number of operation
 */
void mo_queue(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	var.type = QUEUE;
}
