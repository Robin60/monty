#include "monty.h"

/**
 * mo_nop - implement the nop code a no operation
 * @stack: the stack
 * @line_number: the line number of operation
 */

void mo_nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
