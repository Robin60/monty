#include "monty.h"

/**
 * mo_stack - set data format to stack
 * @stack: the double double linked list
 * @line_number: line number of operation
 */
void mo_stack(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	var.type = STACK;
}
